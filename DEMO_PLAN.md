# 五分钟答辩演示流程计划

> 水培农业管理系统 — 论文答辩演示脚本
> 总时长：5分钟 | 角色：管理员 (ADMIN)

---

## 时间分配总览

| 时间段 | 模块 | 时长 | 核心展示点 |
|--------|------|------|-----------|
| 0:00-0:30 | 开场 & 登录 | 30s | 系统入口、角色权限 |
| 0:30-1:30 | 指挥中心大屏 | 60s | 全局态势感知、实时数据 |
| 1:30-2:30 | 实时遥测 | 60s | SSE 推送、InfluxDB 时序、ECharts |
| 2:30-3:30 | 策略与控制 | 60s | 阈值策略、MQTT 下发、气候曲线 |
| 3:30-4:30 | 作物全生命周期 | 60s | 批次管理、生长阶段、采收记录 |
| 4:30-5:00 | 告警 & 收尾 | 30s | 告警工作流、审计追溯 |

---

## 详细演示脚本

### 0:00-0:30 — 开场 & 登录

**操作步骤：**
1. 打开浏览器，访问 `http://localhost:8082`
2. 自动跳转到 `/login` 登录页
3. 输入管理员账号密码，点击登录
4. 进入指挥中心大屏

**口述要点：**
> "本系统采用 JWT 认证，基于 RBAC 三级权限模型——管理员、操作员、观察员。现在以管理员身份登录，拥有系统全部功能权限。"

**备用方案：** 提前登录好，保持 session，万一过期直接刷新页面即可（token 在 localStorage）。

---

### 0:30-1:30 — 指挥中心大屏 (Dashboard)

**操作步骤：**
1. 登录后自动进入 `/` 指挥中心大屏
2. 指向上方 **四个统计卡片**：活跃批次、未处理告警、在线设备、今日能耗
3. 目光移到左侧 **温室实时监控卡片**——温度、湿度、EC、pH、溶解氧、光照
4. 指向右侧 **活跃批次进度表**——当前处于哪个生长阶段、运行天数
5. 视线移动到底部 **最近告警** 和 **最近指令** 表格

**口述要点：**
> "指挥中心是系统的全局态势感知页面。上方四个卡片展示关键指标一览。左侧是每个温室的实时环境参数，包括温度、湿度、电导率、pH 值、溶解氧和光照强度——这些数据通过 MQTT 从传感器实时上报。右侧是当前活跃批次的生长进度。底部展示最近告警和最近下发的控制指令及其执行状态。整个页面的数据通过 SSE（Server-Sent Events）实时推送更新。"

**备用方案：** 如果 SSE 断开，顶部会有红色连接状态提示，可手动点击重连按钮。提前确保 docker 服务正常运行。

---

### 1:30-2:30 — 实时遥测 (Collection Center)

**操作步骤：**
1. 点击侧边栏「采集中心」→「实时总览」进入 `/collection/overview`
2. 在筛选栏依次选择：**温室 → 种植区 → 设备 → 指标**（选温度、湿度两个指标）
3. 页面展示 ECharts 实时折线图，数据点每秒更新
4. 切换时间范围预设（1小时 → 24小时），展示历史趋势
5. 点击「历史趋势」进入 `/collection/trends`，展示更长时间跨度的数据分析

**口述要点：**
> "采集中心是本系统的核心功能之一。与传统农业靠人工巡检读取仪表不同，本系统通过 EMQX 消息中间件接收设备上报的传感器数据，经后端处理后存入 InfluxDB 时序数据库。前端通过 SSE 订阅实时数据流，使用 ECharts 渲染动态折线图。时间范围可从 1 小时切换到 7 天，方便对比日间和夜间的环境变化规律。"

**备用方案：** 如果实时数据没有更新（MQTT 模拟器未运行），打开浏览器控制台展示 SSE 事件流的数据结构，证明通道是通的。

---

### 2:30-3:30 — 策略与控制 (Strategy & Control)

**操作步骤：**
1. 点击「策略中心」→「控制策略」进入 `/strategy/policies`
2. 列表展示已有的阈值策略和定时策略（如"温度>30°C 开启风机"）
3. 点击一条策略进入详情页 `/strategy/policies/:id`
   - 展示 **触发条件**（指标、比较符、阈值）
   - 展示 **执行目标**（目标设备、动作指令）
   - 展示 **执行历史**（最近触发的记录及结果）
4. 点击「策略中心」→「气候曲线」进入 `/strategy/climate`
   - 展示一个多阶段气候曲线（如发芽期→苗期→生长期，各阶段不同温湿度目标）
5. 点击「指令下发」进入 `/strategy/commands`
   - 展示指令列表及执行状态（待发送→已发送→已执行→失败）

**口述要点：**
> "策略中心实现了自动化环境控制。控制策略分为两种：阈值触发和定时执行。当传感器数据满足预设条件时，系统自动通过 MQTT 向执行器下发控制指令——比如温度超过 30 度自动开启风机。气候曲线则定义了作物各生长阶段的目标环境参数，切换生长阶段时自动推送新的配置到设备。所有指令下发都有完整的执行追踪和失败重试机制。"

**备用方案：** 如果设备不在线导致指令无法执行，直接展示策略的配置逻辑和执行历史记录即可。

---

### 3:30-4:30 — 作物全生命周期 (Batch Management)

**操作步骤：**
1. 点击「作物管理」→「批次台账」进入 `/batches/ledger`
2. 列表展示所有种植批次——品种、温室、种植日期、当前阶段
3. 点击一个批次进入详情页 `/batches/:id`
   - **基本信息区**：品种、种植密度、种植日期、预计采收日期
   - **阶段进度**：当前在哪个生长阶段（发芽→幼苗→营养生长→开花→结果），已运行天数
   - **绑定的传感器和执行器设备**
   - **关联的营养液配方**
4. 返回列表，点击「采收记录」进入 `/batches/harvest`
   - 展示历史采收数据
5. 点击「批次复盘」进入 `/batches/review`
   - 展示某个已完成批次的综合分析（产量、能耗、告警汇总）

**口述要点：**
> "作物管理覆盖了从种植到采收的完整生命周期。每个批次记录品种、种植日期、当前生长阶段。生长阶段的切换可以手动或自动触发——切换时会自动推送对应阶段的气候策略和设备配置。采收记录用于追溯产量数据。批次复盘功能在批次结束后汇总产量、能耗、告警等关键指标，为下一轮种植提供数据参考。"

**备用方案：** 如果批次数据较少，可以在详情页多停留，重点讲解字段含义和阶段切换的设计逻辑。

---

### 4:30-5:00 — 告警 & 收尾

**操作步骤：**
1. 先指向上方导航栏的 **告警铃铛图标**——角标数字表示当前未处理告警数
2. 点击进入「告警管理」→「告警列表」进入 `/alerts/list`
   - 按级别筛选：严重(CRITICAL)、警告(WARN)、信息(INFO)
   - 展示告警生命周期：**未处理 → 已确认 → 已解决**
   - 现场确认一条告警
3. （如果时间充足）进入「告警时间线」`/alerts/timeline` 展示可视化时间轴
4. 最后总结一下系统架构亮点

**口述要点：**
> "告警管理采用完整的事件驱动工作流——传感器异常触发告警，通过 SSE 实时推送到前端，导航栏铃铛角标即时提醒。处理人员确认后状态流转为已确认，问题解决后关闭。所有操作记录在审计日志中可追溯。"
>
> "总结一下，本系统的技术亮点包括：MQTT 物联网数据接入、InfluxDB 时序存储、SSE 实时推送、基于事件的策略自动调度，以及 RBAC 权限控制。覆盖了水培农业从环境监控、自动控制到作物管理的完整业务闭环。"

**备用方案：** 如果时间紧张，省略告警时间线，直接进入总结。

---

## 答辩演示准备清单

### 演示前必做检查

- [ ] `docker compose up -d` — MySQL、InfluxDB、EMQX 全部运行
- [ ] 后端服务启动：`cd packages/backend && go run cmd/api/main.go`
- [ ] 前端服务启动：`cd packages/frontend && npm run dev`
- [ ] 浏览器打开 `http://localhost:8082`，确认登录正常
- [ ] 检查数据库是否有足量演示数据（批次、设备、遥测数据、告警）
- [ ] 如果有 MQTT 模拟器，启动并确认数据在实时更新
- [ ] 关闭不必要的浏览器标签页和通知弹窗
- [ ] 浏览器缩放比例调至合适大小（建议 100%-125%），确保表格不溢出
- [ ] 准备一个备用浏览器（Firefox/Edge），以防 Chrome 出现兼容问题

### 如果演示数据不足

考虑提前插入一批模拟数据：
- 2-3 个温室，每个有 2-3 个种植区
- 5-10 个传感器设备（温度、湿度、EC、pH）
- 3-5 个执行器设备（风机、水泵、补光灯）
- 2-3 个活跃批次（不同生长阶段）
- 若干条历史遥测数据（InfluxDB）
- 若干条告警记录（不同级别和状态）
- 3-5 条控制策略（阈值 + 定时）
- 1-2 个气候曲线

### 答辩常见问题预判

| 可能的问题 | 回答要点 |
|-----------|---------|
| 为什么选 InfluxDB？ | 时序数据库专为 IoT 传感器数据优化，写入吞吐高，支持自动过期策略 |
| SSE vs WebSocket？ | SSE 单向推送够用，HTTP 协议兼容性好，浏览器原生支持自动重连 |
| 如何保证指令可靠下发？ | MQTT QoS 1/2 + 指令回执确认 + 失败重试队列 |
| 安全性如何？ | JWT 认证 + RBAC 权限 + 参数化查询防注入 + API 统一响应封装 |

---

## 页面路由快速参考

| 演示顺序 | 路由路径 | 页面名称 |
|---------|---------|---------|
| 1 | `/login` | 登录页 |
| 2 | `/` | 指挥中心大屏 |
| 3 | `/collection/overview` | 实时遥测总览 |
| 4 | `/collection/trends` | 历史趋势分析 |
| 5 | `/strategy/policies` | 控制策略列表 |
| 6 | `/strategy/policies/:id` | 策略详情 |
| 7 | `/strategy/climate` | 气候曲线 |
| 8 | `/strategy/commands` | 指令下发 |
| 9 | `/batches/ledger` | 批次台账 |
| 10 | `/batches/:id` | 批次详情 |
| 11 | `/batches/harvest` | 采收记录 |
| 12 | `/alerts/list` | 告警列表 |
| 13 | `/alerts/timeline` | 告警时间线 |
| 14 | `/users` | 用户管理 |

---

*演示计划制定日期：2026-05-18*
